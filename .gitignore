﻿# Ignore React build output and dependencies
BankStatementAnalytics/wwwroot/
Client/node_modules/
# Local env overrides - never commit; ship Client/.env.example as the template instead
/Client/.env
/.vs/BankStatementAnalytics
/BankStatementAnalytics/bin/Debug/net10.0
/BankStatementAnalytics/obj
/.vs/ProjectEvaluation
/.vs
/BankStatementAnalytics/bin/Release/net10.0
/BankStatementAnalytics/Uploads
/Publish
/Setup
/BankStatementAnalytics/bin
/server.log
/server.err.log
# Real bank statements used for tuning PDF extraction - never commit (contain account numbers)
/samples/
# Private shared libraries - kept locally to rebuild the vendored artifacts, but
# never published (referenced as prebuilt output instead of source). See README.
#   Common.Framework -> BankStatementAnalytics/lib/Common.Framework.dll
#   Common.Client    -> Client/vendor/common-client/ (that vendored copy IS committed)
/Common.Framework/
/Common.Client/
# Local verification tooling only (playwright, used to drive the UI while testing).
# The app's own Node project lives in Client/ - nothing here belongs to the build.
/node_modules/
/package.json
/package-lock.json
