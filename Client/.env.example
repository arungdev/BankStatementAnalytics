# Copy to `.env` for local overrides. Currently optional: the app's axios client uses a
# relative baseURL ("/api"), so this variable is not required for normal dev or production.
# Set it only if you point the SPA at a non-default API origin.
VITE_API_BASE=https://localhost:7187/api
