{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Port the published/Release app (Production environment) listens on. Dev overrides
  // this via launchSettings / appsettings.Development.json to stay on 5000.
  "Urls": "http://localhost:5080",
  "Database": {
    "Provider": "Postgres",
    "Embedded": true,
    "PostgresConnectionString": "Host=localhost;Port=5432;Database=bankstatements;Username=postgres;Password=changeme"
  }
}
